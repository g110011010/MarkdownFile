---
title: 2017-9-26 
tags: 计算机网络
author:sf
grammar_cjkRuby: true
---
<!--teacher info-->
<!--Name:雷友珣-->

## Course 1
1. Computer Networks:
	* a collection of autonomous computers interconnected by a single technology
	* Users are exposed to the atual machines
	* If the machine have different hardware or

2. distributed system:


3. Components of CN:
	* Computers/Hosts/End Systems
	* Communication Links
	* Switches/Routers
4. Business Application
5. Home Apllication
> IETF:互联网标准提出公司

### Network hardware
1. end systems(hosts):run application programs
2. client/server model:client
3. mesh of interconnected routers
	* circuit switching:dedicated circuit per call:telephone net(数据传输一旦联通，网路就被独占，直到本次交互停止)
	* packet-switching:data sent thru net in discrete "chunks"(数据包前面有目标的地址，由数据包自动寻找目标地址)

4. broadcast networks(广播通信):Sending a packet to all destinations,each machine checks the address field.
5. ponint-to-point networks:
6. 体域网（personal area network）:

网络|距离|描述
--|---|--
personal area network|1m|蓝牙通讯，红外通讯，智能手表
Local area network|1Km|
MAN|10KM|城域网
WAN||广域网,提供从LAN(local area network局域网)到互联网的连接,采用电信网络,传输范围更远

7. Chnnel allocation

	* 静态分配：使用时间片轮寻算法，允许每一个用户在轮寻到其位置的时候连接到网络
	* 动态分配

8. MAN & WAN

#### 计算机网络与分布式系统
描述|计算机网络|分布式系统
--|---|--
不同点|在计算机网络中，不存在一致性，模式和软件。用户暴露于真实的机器
1. 传输子网(communication subnet)
	* 传输线
	* 交换设备(路由器
		* 存储和发送
		* 重新组装
		* 路由描述,路由算法

### 无线网(wireless network)
1. 无线网实例:
	* 无线局域网:IEEE 802.11 a/b/c....
	* 系统连接:NFC,蓝牙,"ZigBee BLE"
	* 无线城域网&广域网:IEEE 802.16 WiMAX,Radio networks  used for Cellular telephones(1G 2G 3G 4G 5G....)

2. 无线网连接方式:

	* 通过蓝牙
	* 无线局域网

3. 无线网架构
	* SGSN:serving GPRS support network
	* 基站:BSC(2G) RNC(3G) eNockB(4G 不再需要SGSN)

#### Internetworks
1. Internet:一个内部互联的网络的集合叫做Internetwork或者Internet
2. Gateway(网关):提供硬件和软件上必要的传输
3. ISP:Internet server provider,互联网服务提供商 
4. subnet(子网):连接到路由器上的网络等
5. network:子网和它上面主机的集合
6. miss
7. 网络结构:network of networks
8. CNGI

### 网络软件
1. 协议分层:为了减低设计复杂度,大多数网络是使用栈来分层设计的,每一层都建立在它下面一层的基础之上.
2. 一个机器上的第n层与另一个机器上的第n层进行通讯
3. 协议是两个通讯部分之间关于如何进行通讯的规则 
4. peers:不同机器上对等层上的实体
5. innterface:层栈相邻层之间的接口
6. protocal  stack:每一个对应层之间都有相应的协议
7. network architecture
	1. 网络是由层栈组织起来的
			* 每一层将自己的实现细节隐藏起来而向上向下提供相应的功能
			* 每一层都是一种虚拟机
	2. miss

8.  交换机只检查到数据链路层协议,路由器检查到网络层协议
9.  分层的设计问题
	1.  可靠性
		* 差错控制:能查错和纠错
		* 路由:查找网络上一个合理的路径 

	2. 网络演变

		* 协议分层
		* 寻址或命名:区分发送者和接收者
		* 网络互通性
		* 可扩展性
	3. 资源分配
		* 统计复用,按需分配
		* 流量控制
	4. QoS(服务质量)
	5. Security(安全)

10. 错误控制
	1. 物理线路不够完善
		* 错误校验和错误纠正
		* 接收者如何告诉发送者发送的那些数据是正确的哪些是错误的.
	2. 信息可能是顺序错乱的
		*发送者如何给消息编号
		接收者如何确定哪些消息的顺序是错误的
11. 寻址:每一层需要一种机制来确定发送者和接收者,发送者和接收者是不同机器上的进程
12. 流控制:一个高速的发送者可能使得一个低速的接收者无法正确处理
13. 路由:当多个路线存在的时候,必须选择一条最好的通路.
14. 面向连接和面向非连接
	1. 面向连接:必须首先建立连接,使用连接,最后释放连接
	2. 面向非连接:每一个信息带有完全的地址,并且可以通过这些方式独立的发送到接收者. 

课本笔记：
## Chpater 1 引言
> 十八世纪 机械系统时代
> 十九世纪 蒸汽机时代
> 二十世纪 信息收集,处理与分发

1. ==计算机网络==:通过单一技术互相连接的自主计算机集合,如果两台计算机能够交互信息,则称这两台计算机是互相连接的
2. 计算机网络与==分布式系统==

	* 分布式系统:由一组独立计算机组成.呈现给用户的是一个关联系统.在用户看来,分布式系统只是一个模型或者泛型.通常在操作系统之上有一层软件实现这一泛型,这个软件就称为==中间件==.最著名的分布式系统的例子是==万维网(World Wide Web)==
	* 计算机网络：不存在一致性、模型以及软件。用户看到的是实际的机器，系统并没有努力使得这些机器看起来一样，或者使他们保持一致。如果机器有不同的硬件或者不同的操作系统，那么这些差异对于用户来说是完全可见的。
	* > 分布式系统是建立在网络之上的软件系统.这个软件给与分布式系统以高度的凝聚力和透明性.因此,网络和分布式系统的区别在于软件而不是硬件.

### 1.1 使用计算机网络
#### 1.1.1 商业应用
1. ==资源共享(resource sharing)==:目的是让网络中的任何人都能访问所有的程序/设备,尤其是数据,并且这些资源和用户所出的物理位置无关.
2. ==虚拟专用网络(VPN,Virtual Private Network)==:可以将不同地点的单个网络连接成一个扩展的网络。
3. ==客户机-服务器模型（client-server model）== ![][1]


	  >==服务器（server）==：负责数据存储的高性能计算机
	  >==客户机（client）==：通过客户机可以访问远程数据

4. ![enter description here][2]

	> 网络在其中提供了==通信媒介（communication medium）==
#### 1.1.2 家庭应用
1. ![enter description here][3]
2. 一些形式的电子商务：

标记|全称|举例
--|---|--
B2C|企业对消费者|在线购书
B2B|企业对企业|汽车制造商向供应商订购轮胎
G2C|政府对消费者|政府分发电子税收表单
C2C|消费者对消费者|在线拍卖二手商品
P2P|对等|音乐共享

3. 家庭应用分类：
	* miss
	* 人-人通信
	* 广义的电子商务
	* 娱乐
	* ==普适计算（ubiquitous conputing）==

4. ==射频识别（RFID，Radio Frequency IDentification）==：RFID标签是一种无源芯片（即无电池），大小和邮票差不多。RFID读写器可以用于定位数米范围内的物体。

#### 1.1.3 移动用户
1. 移动计算机无线网络

	* 由电话公司经营的蜂窝网络
	* 基于802.11标准的==无线热点（hotspot）==

2. ==固定无线（fixed wireless）==和==移动无线（mobile wireless）==之间的区别。

![enter description here][4]

3. ==近场通讯（NFC，near field communication）==：手机可以充当RFIC智能卡与RFIC读写器进行交互。
4. ==传感器网络（sensor network）==：由能够感知到现实世界的节点组成

#### 1.1.4 社会问题
1. 

###   1.2 网络硬件
1. 从广义上来讲，目前普遍使用的传输方式有两种：分别是广播式链路和点到点链路
2. ==点到点链路(point-to-point)==：将一对单独的机器连接起来，传输的数据成为短消息，有时也被成为数据包或者包。点到点只有一个发送方和一个接收方，有时也称为==单播（unicasting）==
3. ==广播式链路==：通信信道被网络上的所有机器共享。任何一台机器发出的数据包都能够被所有其他任何机器收到。每个数据包的地址字段指定了预期的接收方。如果包的目的地址就是接收机器，则该机器要处理该数据包。如果包的目的地址是其他机器，则当前地址忽略该包。

	> 广播机制还提供了讲一个数据包发送给所有其他机器的可能。这种传输方式称为==广播（boradcasting）==,有些广播系统还支持给其中的一组机器发送数据，这种方式称==组播（multicasting）==

4. 按照不同尺度对互联处理器的分类：

处理器之间的距离|处理器所在的位置|例子
--|---|--
1m|一米见方|个域网
10m|同一个房间内|局域网
100m|同一栋建筑内|局域网
1Km|同一个园区|局域网
10km|同一个城区|城域网
100km|同一个国家|广域网
1000km|同一个大陆|广域网
10000km|同一个行星|互联网

#### 1.2.1 ==个域网（PAN，Personal Area Network）==
1. 个域网：允许设备围绕着一个人进行通讯，eg:计算机通过无线网络与其外围设备进行连接，蓝牙技术，RFID。

#### 1.2.2 ==局域网（LAN，Local Area Network）==
1. 局域网：是一种私有网络，一般在一所建筑物内或者一所建筑物附近。当局域网用于一个公司的时候，他们就被称为企业网络（enterprise network）
2. 在局域网系统中。每台计算机都装有一台无线调制解调器和一个天线。
3. 每天计算机与一台设备通讯，这个设备称为==接入点（AP，Access Point）==，==无线路由器（wireless router）==，==基站（base station）==。他们主要负责中继无线计算机之间的数据包，还负责中继无线计算机和Internet之间的数据包
4. 无线网的一个标准称为==802.11==，俗称==WiFi==
5. 许多有线局域网的拓扑结构是以点到点链路为基础的。俗称为==以太网（Ethemet）==的==IEEE802.3==是目前最常见的一种有线局域网。
6. ==交换机（switch）==：一个交换机有多个==端口（port）==，，每个端口连接一台计算机。交换机的工作是中继与之连接的计算机之间的数据包，根据每个包中的地址来确定这个数据包要发送给哪个计算机。
7. 无线和有线广播网络可以分为静态设计和动态设计，两种设计取决于如何分配信道。
	* ==静态分配==：一个典型的静态分配方式是将将时间划分为离散的时间间隔（时间槽）。并使用轮询算法，每个机器只有在分配给它的时间槽到来的时候进行广播。
	* ==动态分配==：一个公共信道的动态分配方式可以是==集中式==的，也可以是==分散式==的。在集中式的信道分配方法中，有一个中心实体，例如蜂窝网络中的基站，由这个中心实体来决定接下来谁用信道。在分散式的信道分配方式中，没有中心实体；每个机器必须自行决定是否可以传输数据。

8. ==电线网络(Power-line network)==:允许插入到墙上插座的设备在全房子内广播信息。

#### 1.2.3 城域网
1. ==城域网（MAN,Metropolitan Area Network）==:范围可以覆盖一个城市，最有名的城域网例子是许多城市都有的城市教育网。
2. ![enter description here][5]


####   1.2.4  ==广域网（WAN，Wide Area Network）==
1. ![enter description here][6]
2. ==子网（subnet）==:一组路由器和通信线路的集合，主要负责将数据包从源计算机移动到目标计算机
2. 在大多数广域网中，子网由两个部分构成：传输线路和交换元素。

	* ==传输线路（transmission line）==:负责在计算机之间移动比特。
	* ==交换元素（switching element）==:或者简称为交换机，是专用的计算机，负责连接两条或者两条以上的传输线路。当数据到达一个入境线路的时候，交换元素必须选择一个出境线路将数据转发出去。这些负责交换的计算机现在最常用的名称是==路由器（router）==。
3. 广域网和局域网的区别;

	* 通常在广域网中，主机和子网是由不同的人拥有和经营的
	* 广义网和局域网下路由器连接的网络类型不同。广义网是事实上的==互联网络（internetwork）==,或者符合网络，即由多个网络组成的网络。
	* 广域网和局域网下的子网连接内容不同，子网可以连接单个的计算机，也可以连接整个的局域网。

4. 采用虚拟专用网络（VPN）的WAN
5. 使用一个ISP网络的WAN。

#### 1.2.5 互联网络
1. ==互联网络（internetwork）==:一组相互连接的网络称为互联网络或者互联网（internet）,而全球范围内的因特网（Internet）通常使用首字母大写来表示。
2. 子网、网络和互联网络经常发生混淆。
	* 术语子网通常在广域网的上下文环境中才有意义。它指网络运营商所拥有的一组路由器和通信线路。
	* 一个子网和它的主机集合在一起就构成了一个网络。
	* 当不同的互联网络连接在一起的时候就形成了互联网。

3.==网关（gateway）==：将两个或多个网络连接起来并提供必要的转换的机器，其硬件和软件方面的总称称为网关。
### 1.3  网络软件
#### 1.3.1 协议层次结构
1. 为了降低网络设计的复杂性，绝大多数网络都组织成了一个==层次栈（a stack of layer）==或者==分级栈（a stack of level）==,每一层都建立在其下一层的基础之上。 

2. 一个机器上的第n层与另一个机器上的第n层进行对话，该对话中使用的规则和约定统称为第n层协议。
3. ==协议（protocal）==是两个通讯部分之间关于如何进行通讯的规则 
4. ==对等体（peer）==:不同机器上对等层上的实体
5. ==接口（interface）==:位于每一对相邻层次之间，定义了下层向上层提供哪些原语操作和服务。
6. ==协议栈（protocal  stack）==:一个特定的系统所使用的一组协议，即每一层一个协议，称为协议栈。
7. ==网络体系结构（network architecture）==
	1. 网络是由层栈组织起来的
			* 每一层将自己的实现细节隐藏起来而向上向下提供相应的功能
			* 每一层都是一种虚拟机
	2. miss
8. 协议层次的较低层次往往由硬件或者固件实现。
8.  交换机只检查到数据链路层协议,路由器检查到网络层协议
#### 1.3.2 层次设计问题
9.  分层的设计问题
	1.  可靠性
		* 差错控制:能查错和纠错
		* 路由:查找网络上一个合理的路径 
		* >检错: 具有从接受到的信息中发现错误的能力,发现错误后要求重新发送.
		* > 纠错: 从最初收到的可能不准确的信息中恢复正确的消息.

	2. 网络演变

		* 协议分层
		* 寻址或命名:区分发送者和接收者
		* 网络互通性
		* 可扩展性
	3. 资源分配
		* 统计复用,按需分配
		* 流量控制
	4. QoS(服务质量)
	5. Security(安全)

10. 错误控制
	1. 物理线路不够完善
		* 错误校验和错误纠正
		* 接收者如何告诉发送者发送的那些数据是正确的哪些是错误的.
	2. 信息可能是顺序错乱的
		*发送者如何给消息编号
		接收者如何确定哪些消息的顺序是错误的
11. 寻址:每一层需要一种机制来确定发送者和接收者,发送者和接收者是不同机器上的进程
12. 流控制:一个高速的发送者可能使得一个低速的接收者无法正确处理
13. 路由:当多个路线存在的时候,必须选择一条最好的通路.
#### 1.3.3 面向连接和面向非连接

1. ==面向连接的服务(connection-oriented service)==:是按照电话系统建模的,必须首先建立连接,使用连接,最后释放连接
2. ==无连接服务(connectionless service)==:按照邮政系统建模。每一个信息带有完全的目标地址,每个报文都有系统的中间节点路由，而且路由独立于后续的报文，并且可以通过这些方式独立的发送到接收者
。==报文（message）==在不同的上下文中有不同的称呼：==数据包/包(packet)==是网络层的报文。
3. 每个服务都可以使用一个==服务质量（quality of service）==来表述其特征。有些服务是可靠的，意味着它从来不丢失数据。
4. ==IP语音（Voice over IP）==
5. ![enter description here][7]

#### 1.3.4 服务原语
1. 一个服务由一组==原语（primitive）==正式说明。用户进程通过这些原语（操作）来访问该服务。
2. ![enter description here][8]
3. ![enter description here][9]
#### 1.3.5 服务与协议的关系
1. 服务是指某一层向他的上一层提供的一组原语。服务定义了该层准备代表其用户执行哪些操作，但他并不涉及这些操作。服务与两层之间的接口有关，底层是服务提供者，而上层是服务用户。
2. 协议是一组规则，规定了同一层上对等实体之间所交换的数据包或者报文的格式和含义。对等实体利用协议来实现他们的服务定义。
3. ![enter description here][10]
4. 

### 1.4 参考模型
1. 接下来将讨论两种重要的网络体恤解构：OSI参考模型和TCP/IP参考模型

#### 1.4.1 OSI参考模型
1. ![enter description here][11]
2. ==物理层（physical layer）==：关注在一条通信信道上传输原始比特。比如使用什么样的电子信号来表示0和1，一个比特持续多少个纳秒、传输是否可以在两个方向上同时进行，初始连接如何建立等问题。
3. ==数据链路层（data link layer）==: 

	* 将一个原始的传输设施转变成一条没有漏检错误的线路。数据链路层完成这项任务的做法是将真是的错误掩盖起来，使网络层看不到
	* 避免一个快速发送方用数据“淹没”一个慢速接受方。所以，往往需要一种流量调节机制，以便让发送方知道接收方何时能够接受更多的数据。

4. ==网络层（network layer）==：主要功能是控制子网的运行，一个关键的设计问题是如何将数据包从源端路由到接收端。路由可以建立在静态表的基础上，这个表相当于网络内部的“布线”，而且很少会改变。更常见的情况是路由可以自动更新，以避免网络中的故障组件。路由也可以在每次回话开始的时候就确定下来，比如登陆到一台远程的机器上。路由可以是高度动态的，针对每一个数据包都重新确定路径，以此来反映网络当前的负载情况。
5. ==传输层（transport layer）==:

	* 基本功能是接受来自上一层的数据，在必要的时候把这些数据分割成一些较小的单元，然后把这些单元传输给网络层，并确定这些单元正确的到达了另一端。
	* 传输层还决定了向会话层，因而是实际的最终网络用户提供哪种类型的服务。其中最常见传输连接是一个完全无错的点到点信道。、

6. ==会话层（session layer）==：允许不同机器上的用户建立会话。同步管理
7. ==表示层（presentation layer）==：表示层以下的各层关注的是=如何传递数据位，而表示层关注的是所传递信息的语法和语义。
8. ==应用层（application layer）==：包含了用户通常需要的各种各样的协议。一个得到广泛使用的用户协议是超文本传输协议（HTTP，HyperText Transfer Protocal）,它是万维网的基础。

#### 1.4.2 TCP/IP参考模型
1. ==互联网层==:一个分组交换的网络,在这一层定义了IP(Internet Protocol),避免拥塞,网络之间的差异被完全屏蔽在这个网络层.
2. ==传输层==:TCP,  <br>UDP:面向非连接,不可靠,快速实时,但不保证完全正确b
3. ==应用层==
#### 1.4.3 本书中使用的模型
#### 1.4.4 OSI参考模型与TCP/IP参考模型的比较
#### 1.4.5 OSI参考模型的评判
#### 1.4.6 TCP/IP参考模型的评判
### 1.5 网络实例
#### 1.5.1 因特网
#### 1.5.2 第三代移动电话网络
#### 1.5.3 无线局域网:802.11
#### 1.5.4 RFID和传感器网络

### 1.6 网络标准化 
#### 1.6. 电信领域有影响力的组织
#### 1.6. 国际标准领域有影响力的组织
#### 1.6. Internet标准领域有影响力的组织

### 1.7 度量单位
### 1.8 本书其余部分概要
### 1.9 本章总结



## Heading








































  [1]: ./images/1506783598392.jpg
  [2]: ./images/1506784220420.jpg
  [3]: ./images/1506784391294.jpg
  [4]: ./images/1506785828580.jpg
  [5]: ./images/1506933310880.jpg
  [6]: ./images/1506933630795.jpg
  [7]: ./images/1507347062554.jpg
  [8]: ./images/1507347279530.jpg
  [9]: ./images/1507347308459.jpg
  [10]: ./images/1507348139096.jpg
  [11]: ./images/1507443877192.jpg